using namespace std;
#include <bits/stdc++.h>

int main(void)
{
    int N, M;
    cin >> N >> M;

    vector<int> A(M), B(M);

    for (int i = 0; i < M; i++)
    {
        cin >> A.at(i) >> B.at(i);
    }

    vector<char> data()

    return 0;
}
