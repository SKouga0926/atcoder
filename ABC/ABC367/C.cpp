using namespace std;
#include <bits/stdc++.h>

typedef long long ll;

int main(void)
{
    // 入力
    int N, K;
    cin >> N >> K;

    int R[N];
    for (int i = 1; i <= N; i++) cin >> R[i];

    

    return 0;
}
